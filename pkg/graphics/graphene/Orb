use Orbital -type => 'define', -version => 'v0';

define 'package', sub {
	metadata debian_apt => { name => 'gir1.2-graphene-1.0' };
	metadata macos_homebrew => { name => 'graphene' };
	metadata msys2_pacman => { name => 'graphene' };
}
