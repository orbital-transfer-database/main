use Orbital -type => 'define', -version => 'v0';

define 'package', sub {
	metadata debian_apt => { name => 'libcairo2' };
	metadata macos_homebrew => { name => 'cairo', phase => 'dev|run' };
	metadata msys2_pacman => { name => 'cairo' };
};
