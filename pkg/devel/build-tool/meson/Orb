use Orbital -type => 'define', -version => 'v0';

define 'package', sub {
	metadata debian_apt => { name => 'meson' };
	metadata macos_homebrew => { name => 'meson' };
	metadata msys2_pacman => { name => 'meson' };
};
