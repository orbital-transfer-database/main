use Orbital -type => 'define', -version => 'v0';

define 'package', sub {
	metadata debian_apt => { name => 'cmake' };
	metadata macos_homebrew => { name => 'cmake' };
	metadata msys2_pacman => { name => 'cmake' };
};
